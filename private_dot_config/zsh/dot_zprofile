typeset -U path

export DOTFILES="${HOME}/.local/share/chezmoi"
export DOTFILES_MODULES="${DOTFILES}/.modules"

path=("${HOME}/.local/bin" "${DOTFILES}/.scripts.sh" ${path[@]})

# vim: set filetype=zsh :
